function createDeck()

function shuffleDeck()

function splitDeck()

function battleWinner()

function startGame() {
// clicking will create the deck, shuffle the deck and assign 26 cards to each player
}

function playGame() {
// clicking 'let's battle' will pull top card from each deck, determine winner and add both cards to bottom of winner deck
}
